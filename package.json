{
  "name": "voting-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm i && cd react-web && npm i  && cd node_modules/semantic && gulp build && exit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.6.2",
    "koa-static-server": "^1.3.4"
  },
  "devDependencies": {
    "nodemon": "^1.18.7"
  }
}
